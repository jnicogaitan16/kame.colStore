/** Inputs `type="date"` en cabeceras del admin: ancho completo solo en móvil muy estrecho; desde sm ancho fijo + flex-none para que no estiren la fila. */
export const ADMIN_DATE_INPUT_CLASS =
  "w-full min-w-0 max-w-full flex-none rounded-lg border border-zinc-200 bg-white px-2 py-2 text-sm shadow-sm tabular-nums " +
  "sm:w-[9.5rem] sm:max-w-[9.5rem] sm:py-1.5 sm:text-xs " +
  "focus:outline-none focus:ring-2 focus:ring-red-500/15 focus:border-red-300";
